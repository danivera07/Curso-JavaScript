var nombreDeLaVariable = 'Curso de javascript'
let nombreDeVariable = 'aprendiendo javascript'
const nombreVariable = 'con sergie code'


let x, y, z;
x = 5;
y = 10;
z = x + y;
console.log(z);

/**ANOTACIONES
 * Al definir variables con var y let se permite no asignarles ningun valor pero con const si lo requiere 
 
 * SCOPE: Es el ambiente en el que va a estar disponible el codigo
  - var : si se declara dentro de una funcion solo es accesible dentro de esa funcion y si se declara fuera es global 
  - let y const : solo son accesibles dentro de la funcion en la que se declararon

 * En las variables let y var se puede reasignar y en el caso de la variable const no se puede

 * Con let se puede declarar varias variables al mismo tiempo
  
 * Las variables let y const solo estaran disponibles dentro del bloque donde fue declarada 
 */